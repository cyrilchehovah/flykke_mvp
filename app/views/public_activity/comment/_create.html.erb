<div class="activity-comment">
  <% if activity.trackable %>
    <div class="info">
      <%= render 'shared/avatar', user: activity.owner %>
      <h4 class="name"><%= activity.owner.first_name %> <%= activity.owner.last_name %></h4>
      <span class="action">commented on this creation</span>
      <%= render 'shared/created_at', activity: activity %>
      <!-- activity.trackable.commentable renvoie nil => il faut trouver pourquoi et le régler!! -->
    </div>
    <div class="content">
      <%= render 'posts/post', post: activity.trackable.post %>
      <h4><small><%= activity.trackable.body %></small></h4>
    </div>
    <% end %>
</div>



